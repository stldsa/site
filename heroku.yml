setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
release: 
  image: web
  command: 
   - django-admin migrate
  asset-syncer:
    command:
      - django-admin collectstatic
    image: web
  