setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
release: 
  image: web
  command: 
   - /usr/local/lib/rye/shims/rye sync --no-dev --no-lock && /usr/local/lib/rye/shims/rye run django-admin migrate
