from django.urls import path

from . import views

urlpatterns = [
    # path('', views.CommitteeList.as_view(), name='committees'),
    # path('<slug:slug>/', views.CommitteeDetailView.as_view(), name='committee-detail'),

]